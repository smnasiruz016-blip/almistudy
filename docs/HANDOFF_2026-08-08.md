# AlmiStudy — handoff, 2026-08-08 (end of day)

---

# ▶▶ UPDATE — THE 11-COUNTRY PASS IS MERGED AND BUILT (not yet pushed)

Everything below the line was the plan. This is what happened.

**Dataset 4,277 → 5,445 rows (+1,168).** All 11 pending files merged in one pass, India
first, `next build` green, all three gates green, every count verified in the built HTML.
**The commit is made but NOT pushed** — see the spend cap note at the end.

| file | rows in | added | skipped as dupes | country now |
|---|---|---|---|---|
| `_India_additions_739.json` | 739 | 683 | 56 | india **780** |
| `_Turkey_additions_198.json` | 198 | 170 | 26 + 2 dropped | turkiye **200** |
| `_Iran_additions_91.json` | 91 | 52 | 38 + 1 dropped | iran **96** |
| `_Iraq_additions_107.json` | 107 | 77 | 29 + 1 dropped | iraq **119** |
| `_Algeria_additions_66.json` | 66 | 65 | 1 dropped | algeria **68** |
| `_SaudiArabia_additions_44.json` | 44 | 18 | 25 + 1 dropped | saudi-arabia **49** |
| `_UAE_additions_33.json` | 33 | 15 | 17 + 1 dropped | united-arab-emirates **43** |
| `_Sudan_additions_36.json` | 36 | 36 | 0 | sudan **42** |
| `_Morocco_additions_24.json` | 24 | 22 | 2 dropped | morocco **26** |
| `_Cameroon_additions_16.json` | 16 | 16 | 0 | cameroon **21** |
| `_Senegal_additions_14.json` | 14 | 14 | 0 | senegal **18** |
| **total** | **1,368** | **1,168** | **191 + 9 dropped** | **5,445** |

India is now the largest country in the dataset at 780, overtaking Pakistan's 287.

## ⚠️ THE ONE THAT WOULD HAVE SHIPPED BROKEN: Turkey is live as `turkiye`

`_Turkey_additions_198.json` declares `country.slug "turkey"`, `country.name "Turkey"`,
`region "Europe"`. **Live is `turkiye` / "Türkiye" / `Asia`, 30 rows.** The merge script
keys dedupe on `(country.slug, slug)`, so it saw a different country and would have
appended all 198 rows. That produces two failures at once:

1. **A second Türkiye country page** at `/universities/turkey`, listing 198 universities
   in Europe while `/universities/turkiye` listed 30 in Asia. 198 countries, not 197.
2. **26 global `/university/[slug]` clashes.** `istanbul-university`,
   `bogazici-university`, `hacettepe-university` and 23 more already exist under
   `turkiye`. The route is global, so each new row appends cleanly and then serves no
   page — the documented "a cross-country clash LOSES its page" defect, 26 times.

Fix applied to the NEW rows only: `country` copied verbatim from live `UNI_1575`,
`region` set to `Asia`. Live rows untouched. The 26 then deduped normally.

**This is the real lesson of the pass: the pre-flight's per-country near-duplicate scan
was BLIND to Turkey while the country key was wrong** — the comparison pool for "turkey"
never contained the live "turkiye" rows. Remapping the identity exposed two more
same-institution duplicates that no earlier check could have seen
(`hatay-mustafa-kemal-university` vs live `mustafa-kemal-university-hatay`,
`tobb-university-of-economics-and-technology` vs live
`tobb-economics-and-technology-university`). **Check the country key before trusting any
per-country metric.** A near-duplicate scan is only as good as the bucket it groups by.

## Cross-country slug clashes — 3 renamed (new row only, never the incumbent)

| new row | clashed with | renamed to |
|---|---|---|
| turkey `national-defence-university` | live pakistan `UNI_3812` | `national-defence-university-turkiye` |
| turkey `suleyman-demirel-university` | live kazakhstan `UNI_1131` | `suleyman-demirel-university-turkiye` |
| iraq `national-university-of-science-and-technology` | live zimbabwe `UNI_0541` | `national-university-of-science-and-technology-iraq` |

After the fixes the only global slug collision left in 5,445 rows is the **pre-existing**
Jordan `UNI_0618` / Lebanon `UNI_1229` `middle-east-university` pair, which is why
`universities()` reports 5,444 servable pages for 5,445 rows. Unchanged by this pass.

## Dedupe-evading variants — 9 new rows dropped

Same institution, same city, slug differing by a dropped stopword or an appended
acronym. The `(country, slug)` key cannot see these. In every case the LIVE row stays.

| dropped new row | duplicate of live |
|---|---|
| turkiye `hatay-mustafa-kemal-university` | `mustafa-kemal-university-hatay` |
| turkiye `tobb-university-of-economics-and-technology` | `tobb-economics-and-technology-university` |
| iran `islamic-azad-university-science-and-research-branch` | `islamic-azad-university-science-and-research` |
| iraq `imam-jaafar-al-sadiq-university` | `university-of-imam-jaafar-al-sadiq` |
| algeria `university-oran-1-ahmed-ben-bella` | `university-of-oran-1-ahmed-ben-bella` |
| saudi-arabia `king-saud-bin-abdulaziz-university-for-health-sciences` | `…-for-health-sciences-ksau-hs` |
| united-arab-emirates `university-of-wollongong-in-dubai` | `university-of-wollongong-dubai` |
| morocco `hassan-ii-university-casablanca` | `hassan-ii-university-of-casablanca` |
| morocco `al-akhawayn-university-ifrane` | `al-akhawayn-university-in-ifrane` |

**Four near-duplicate pairs were reviewed and KEPT** — the metric flagged them, judgment
cleared them. Iraq `al-imam-university-college` (Balad) is not
`imam-al-aadham-university-college` (Baghdad). India's
`babasaheb-bhimrao-ambedkar-university` (Lucknow) is not
`babasaheb-bhimrao-ambedkar-bihar-university` (Muzaffarpur);
`central-university-of-andhra-pradesh` (Anantapur) is not
`central-tribal-university-of-andhra-pradesh` (Vizianagaram);
`atal-bihari-vajpayee-hindi-vishwavidyalaya` (Bhopal) is not
`atal-bihari-vajpayee-vishwavidyalaya` (Bilaspur). The ≥0.8 scan is a review list, as
predicted — 13 pairs surfaced, 9 were defects.

## Converter status: clean on the four it was fixed for, one NEW defect

The upstream converter fix **is** holding on all four: **0 doubled region tags, 0 null
cities** (1 empty Turkey city), **0 phones, 0 emails** across all 1,368 rows. Also 0
`example.edu`, 0 markdown `**`, 0 duplicate website URLs, 0 truncated `source.id`.

Two hygiene defects were found and fixed in the new rows only:

1. **`Ministere de lEnseignement superieur`** — the francophone ministry name lost its
   apostrophe and both accents. 92 occurrences across 22 Morocco + 14 Senegal rows, **0
   in the 4,277 live rows, so this is new**. It matters more than the Egypt doubled tags
   did: `studentNotes` **renders** on both `/universities/[country]` and
   `/university/[slug]`, so this one would have reached a reader. Fixed to
   `Ministère de l'Enseignement supérieur` (casing preserved per row) by exact-string
   replacement with asserted counts — no regex over prose. Verified 0 occurrences remain
   in the built HTML.
2. **16 India rows carried a categoryTag twice**, differing only by a trailing period
   (`"Central university"` + `"Central university."`). Same latent class as the Egypt
   tags. De-duplicated period- and case-insensitively.

## Surface, gates, build

- **35,468 URLs** = **7,815 prebuilt** + **27,653 bounded-on-demand** (was 30,542 =
  6,640 + 23,902).
- `lib/isr-policy.ts` `approxPages` updated 23,902 → **27,653**.
- ISR cost gate ✓ · sitemap==servable gate ✓ (35,468 == 35,468) · uniqueness gate ✓.
- `next build` green, 7,821 static pages in 8.4 min, `/university/[slug]` prebuilt 5,444.
- **Counts verified in the BUILT HTML**, not just the data: all 11 countries render the
  new `numberOfItems` and title. `universities/turkey.html` does not exist; `turkiye.html`
  reads 200.
- `npm run validate:data` error **classes** unchanged — all six are the `got null`
  contact-shape classes (null>fabrication doctrine). The 2 `http://` `scholarshipsUrl`
  errors are **pre-existing** (Chad `UNI_0274`, Eritrea), not from this pass.
- **Static satellite regenerated**: `almistudy_generate_v2.py` run against the merged
  file → **5,643 pages** (197 country + 5,445 university + index) in
  `C:\Users\Lenovo\almistudy-build\almistudy_site\`, project `almistudy-pages`. This was
  the first run of the v2 competitor-grade template against live data — spot-checked page
  is 957 words with FAQPage + BreadcrumbList + CollegeOrUniversity JSON-LD. **Not
  deployed** (separate Vercel project, separate deploy).

## Tooling change

`_merge_additions.py` in the data folder was **patched `indent=1` → `indent=2`**, the
pre-flight's own item 8. The diff is now provably append-only: **63,327 insertions, 0
deletions**, and the first 4,277 rows compare byte-identical to `HEAD`. Line endings were
re-normalised to CRLF to match the repo's working-copy convention.

## ⚠️ WHY THIS IS NOT PUSHED — the spend cap is the blocker, not the code

Checked before starting, read-only, per doctrine #4 (the agent does not touch billing):

| | |
|---|---|
| plan | Pro, team `almiworld` |
| cycle | 2026-07-27 → 2026-08-27, **18 days remaining** |
| included credit | $20.00 / $20.00 — spent |
| on-demand charges | **$56.92** |
| **on-demand budget** | **$56.92 / $70 — 81%** |
| **Pause Projects** | **ON** |
| upcoming invoice | $76.92 |

**$13.08 of headroom, 18 days left, and Pause Projects is ON** — that switch is what
produced the 12-subdomain `DEPLOYMENT_PAUSED` event. The two largest lines are **ISR
Writes $32.58** and **Observability Events $23.05**, so it is serving and ISR that is
consuming the budget, not build minutes. This pass raises the bounded-on-demand route
from 23,902 to 27,653 ISR writes per deploy — a permanent increase of ~3,750 writes,
re-armed on every deploy.

**Founder's call before the push.** Nothing in the repo needs changing to recover.

## Next pass

- The bounded-ISR ceiling decision is now closer: **27,653 of 50,000**, 5.08 taught pairs
  per university, so the ceiling arrives at roughly **9,800 universities** — two or three
  passes away. Noted in `lib/isr-policy.ts` itself.
- **`established` is null on all 5,445 rows** while `studentNotes` states "Founded YYYY"
  on 1,146 of the 1,168 new ones. The year is present as prose and absent as structured
  data, so JSON-LD emits no `foundingDate`. Pre-existing across the whole dataset (0 of
  4,277 live rows populate it), so this is a converter/enrichment gap, not a regression —
  and populating it from the notes is a decision, not a cleanup.
- **Website coverage: all 1,368 incoming rows have `website: null`.** Never guessed,
  per doctrine. This is now the dominant enrichment backlog.
- India carries **312 rows with fewer than three subjects**; Algeria 6 and Cameroon 3
  rows have zero. Pre-existing tolerated state — the renderer omits the section.

---

## ▶ ORIGINAL PLAN (2026-08-08, superseded above)

## ▶ NEXT SESSION: START WITH INDIA (739)

Founder's call, 2026-08-08: begin the next pass with `_India_additions_739.json`, then the rest of
the pending list below.

Pre-checked tonight so you can start cold:

- **India is live with 97 rows**, `country.slug = "india"`, `region = "Asia"` — matches the batch
  exactly, no slug or region decision needed.
- **No flagged India rows**, so there is no promote-not-duplicate step (unlike the Day 6 India batch,
  which had 5).
- Batch is **739 rows**, single country object, `region: Asia` throughout.
- **The converter fix is holding**: 0 doubled region tags, 0 null cities, 0 phones, 0 emails. This is
  the first batch to arrive clean on all four — confirm the same for Turkey/Iraq/Iran before trusting
  it wholesale.
- ⚠️ **47 slugs end in a short alphabetic token**, which is the shape that hid tonight's 12
  dedupe-evading duplicates (`...-lums`, `-ucp`, `-cust`). Most will be legitimate place names
  (`-delhi`, `-pune`), so this is a *review list*, not 47 defects. Run the strict near-duplicate scan
  (full-slug token similarity ≥0.8, per country, live + batch) before merging — India at 97 live +
  739 incoming is the highest-risk pass yet for this defect.
- India will become the **largest country in the dataset** (up to 836, less exact and variant dupes),
  overtaking Pakistan's 287. Expect `added < 739` and report the real number.
- Budget the time: 739 rows is larger than tonight's entire 12-file pass, each merge rewrites the
  whole ~16MB file, and the build is now ~7 minutes.

⚠️ **Before deploying anything, check whether the 11 paused products were un-paused** — see the
production alert below. A push that cannot deploy will look like a successful commit.

## LIVE NOW

- **Dataset: 4,277 rows** in `lib/universities-publishable.json` (was 3,197 this morning).
- **17 countries enriched today**, all verified on production, not just in the build:

  | pass | countries | added |
  |---|---|---|
  | `c510b7d` | Nigeria 3→222 | +219 |
  | `752d50a` | Kenya 4→79 · Ghana 4→55 · Uganda 13→29 · Zimbabwe 3→20 · Ethiopia 3→56 | +212 |
  | `fd9d1c6` | Pakistan 74→287 · Egypt 99→119 · Tanzania 10→36 · Zambia 5→32 · Rwanda 20→39 · Bangladesh 14→148 · Nepal 25→31 · Sri Lanka 28→44 · Indonesia 84→113 · Vietnam 32→124 · Philippines 16→83 | +649 |

  Total **+1,080 universities** across 17 countries.
- Surface: **30,542 URLs** = 6,640 prebuilt + 23,902 bounded-on-demand. All three gates green,
  `next build` green, 4,276 university pages.
- Every country page verified live: title, meta description, JSON-LD `numberOfItems` and stat tile
  all render the new count.

## ⚠️ PRODUCTION ALERT — 11 products still paused, not caused by this work

At 04:24 UTC, **12 of 13 almiworld.com subdomains returned HTTP 503 with
`X-Vercel-Error: DEPLOYMENT_PAUSED`**. The founder un-paused AlmiOET at ~04:30 and it is
confirmed back at 200, with its nurse-uk surface intact.

**As of 04:35 UTC, 11 products are STILL PAUSED:** almiprep, almipte, almidet, almicelpip,
almigoethe, almifrench, almispanish, almijob, almisalary, world (parent hub), almihq. Only
almistudy and almioet serve 200. The un-pause was per-project, so each of the 11 still needs
doing.

`DEPLOYMENT_PAUSED` is an account/project-level state, not a code fault — usually a Vercel
spend/usage limit being reached, or projects paused manually. No repo in the network was touched
tonight except almistudy. Three large AlmiStudy builds ran today, which consume build minutes and
may have contributed to reaching a cap; that is a possibility, not a diagnosis.

**Per doctrine #4 the agent does not touch billing or account settings — this is the founder's
call.** Check the Vercel spend-limit / usage page, then un-pause the remaining 11. Nothing in the
repos needs changing to recover. Re-verify with:
`for h in almiprep almipte almidet almicelpip almigoethe almifrench almispanish almijob almisalary world almihq; do curl -sI https://$h.almiworld.com/ | grep -i x-vercel-error; done`

## PENDING — clean additions files, staged but NOT merged

In `C:\Users\Lenovo\OneDrive\Desktop\almiworld\almi cleand uni data\`, all present on disk:

| file | rows |
|---|---|
| `_India_additions_739.json` | 739 |
| `_Turkey_additions_198.json` | 198 |
| `_Iraq_additions_107.json` | 107 |
| `_Iran_additions_91.json` | 91 |
| `_Algeria_additions_66.json` | 66 |
| `_SaudiArabia_additions_44.json` | 44 |
| `_Sudan_additions_36.json` | 36 |
| `_UAE_additions_33.json` | 33 |
| `_Morocco_additions_24.json` | 24 |
| `_Cameroon_additions_16.json` | 16 |
| `_Senegal_additions_14.json` | 14 |
| **total** | **1,368** |

The region-tag and city bugs were fixed **at the source converter**, so these files should be clean.
**Verify, do not assume** — run the pre-flight before merging (below). India at 739 rows is by far
the largest batch shipped so far and Turkey/Iraq/Iran are all sizeable, so expect the merge itself
to take a while: each merge rewrites the whole ~16MB file, and a single merge of the full set took
over 2 minutes tonight.

### Pre-flight to run before merging (all of it earned its place today)

1. **Row counts** per file vs the filename claim, and one `country.slug` per file.
2. **Schema**: no keys absent from live, no unknown keys.
3. **Exact same-country collisions** — the merge script skips these, so `added < rows in`. Egypt was
   64% redundant tonight (36 of 56 already live). Report the real added number.
4. **GLOBAL slug clashes**, cumulatively against live *and* every pending batch. `/university/[slug]`
   is a global route, so a cross-country name collision appends fine and then serves no page.
   Rename the NEW row only (`-bangladesh`, `-kenya`, …), never the incumbent — that would change a
   live URL.
5. **Dedupe-evading slug variants** — the same institution with an acronym appended
   (`...-university-lums`) or a stopword dropped (`national-university-modern-languages`). 12 found
   tonight. Score the **FULL slug** at ≥0.8 token similarity per country. Do **not** stopword-strip
   university/of/national/science/technology: that metric reported 387 false pairs because
   `hanoi-university-of-science-and-technology` and `hanoi-university` both collapse to `{hanoi}`.
6. **Fabrication gate**: phones, emails, `example.edu`, duplicate website URLs, root-domain URL
   copying, truncated `source.id`, markdown `**` in prose, duplicate `categoryTags`, and that
   `accreditationRegistryUrl` is the real national regulator.
7. **Doubled region suffix** in `categoryTags` — collapse **any** repeated word run, not a fixed
   word list (see the Egypt miss below).
8. **`indent=2`** when writing: the merge script hardcodes `indent=1`, which reformats all ~16MB.
   `indent=2` with no trailing newline round-trips the live file byte-exact, so the diff stays an
   append. Assert the first N rows are unchanged after every merge.
9. After merging: update `lib/isr-policy.ts` `approxPages` to the real bounded count, run the three
   gates, run the full `next build`, verify each country's count **in the built HTML**, then commit.

Scripts used tonight live in the session scratchpad; `_merge_additions.py` and
`_fix_region_tags.py` are in the data folder.

## OPEN DECISIONS

**(a) Converter bugs — fixed at source, so verify rather than re-patch.** Two generator defects were
patched per batch tonight before being fixed upstream:
- *doubled region suffix* — "Region: Lusaka Province Province", "Region: Cairo Governorate
  Governorate". Hit 100% of some countries' rows and 0% of others, purely on whether the region
  value already ended in the suffix word.
- *acronym-suffixed slugs* — the dedupe-evading duplicate described above.

Future files are expected clean. Confirm with the pre-flight; if either reappears, the converter
regressed.

**(b) Bounded-ISR ceiling.** `lib/isr-policy.ts` declares the `/university/[slug]/[subject]` route at
**23,902 pages** against `scripts/isr-cost-gate.mts`'s `MAX_BOUNDED_PAGES = 50_000`. At ~5.6
taught-subject pairs per university that ceiling arrives at roughly **9,000 universities** — and the
pending 1,368 rows take the dataset to ~5,600. So this needs a decision within the next two or three
passes, not urgently tonight. The gate's own comment is the right frame: the ceiling exists so a route
this size gets a decision rather than a wider exemption. Options are to shrink what the route
advertises, split it, or raise the ceiling deliberately with a written reason.

## OTHER REPOS — untouched, verified

Nothing outside `almistudy` was modified today.

- **`C:\Projects\almi-oet`** — on branch `nurse-uk-test`, working tree clean, HEAD `0369cb9` dated
  **2026-08-07 19:05**, i.e. yesterday. No commits tonight.
- **The nurse-uk GSC test surface is intact**: `sitemap-nurse-uk.xml` serves its **15** URLs and
  spot-checked pages return 200. Note the paths use the **nationality adjective** —
  `/nurse/indian/uk`, not `/nurse/india/uk` (the latter correctly 404s).
- **The old ~240k OET surface** is likewise untouched; it lives in that repo and no deploy was made
  from it. The sitemap index was unreadable during the pause window, so re-confirm the chunk count
  from the dashboard or a fresh fetch if you want a number rather than "unchanged by us".

## KNOWN OPEN ITEMS (small, none blocking)

- **Jordan `UNI_0618` and Lebanon `UNI_1229` both use the slug `middle-east-university`.** On the
  global `/university/[slug]` route one shadows the other, so 4,277 rows produce 4,276 pages. Fixing
  it changes a live URL and needs its own redirect decision. Pre-existing.
- **Green University of Bangladesh** is recorded as `Dhaka`; its permanent campus is Purbachal,
  Narayanganj. A which-campus question — settle against the UGC address.
- **Madhesh University (Birgunj, Nepal)** could not be confirmed by search. Not contradicted either,
  so it ships unverified rather than asserted.
- **Website coverage is the main enrichment backlog.** Nulls were never guessed: Nepal 6/6 null,
  Bangladesh 74/135, Pakistan2 70/131, Ethiopia 41/53, Zambia 18/27, Nigeria 117/219.
- **8 rows carry zero subjects and ~90 fewer than three**, concentrated in Pakistan2, Bangladesh and
  4 Kenyan constituent colleges. Zero-subject rows are a pre-existing tolerated state and the
  renderer omits the section rather than breaking.
- **4 dropped Pakistan duplicates carried 1–2 more subjects** than the incumbent they duplicated. Not
  merged, because editing live rows would break the append-only property. Enrichment backlog.
- **`npm run validate:data` reports ~10,100 errors and is NOT in `build`.** It rejects explicit `null`
  as a shape error, so every contact-less row scores 6. That is the null>fabrication doctrine, not a
  defect — check the error **class** is unchanged, never the count.

## THE EGYPT MISS — worth reading before the next pass

Tonight's per-batch tag fix used a fixed word list (`Region|Province|County|State|District|Division`)
and **missed `Governorate`**, so 20 Egypt rows shipped to production with
"Region: Cairo Governorate Governorate". Caught afterwards and fixed by `_fix_region_tags.py`, which
collapses **any** repeated word run instead of a named list — the right shape.

**Correction on severity.** Earlier commit messages today (`752d50a`, `fd9d1c6`) called the doubled
tags "user-visible text". They are not. Verified against production: `Region: …` tags render nowhere —
`grep 'Region:'` and `grep 'Governorate'` both return 0 on a live university page, and `categoryTags`
appears in `lib/page-relations.ts` only as a field on the `University` type, with no page printing it.
So this was **latent data hygiene, not a visible defect**: worth fixing so the strings are correct
wherever they are eventually used, but it never reached a reader. Treat it as low priority if it
recurs, not as a production text bug.

That script is also correctly **scoped to tags starting `Region: `**, and that scoping is load-bearing:
three tags elsewhere contain legitimate repeated words — `Vanity-marketing phone (7799 7799)`,
`Dr. Hassan Hassan founded`, and `Federal public public-health degree-awarding institute.` A
dataset-wide collapse would have corrupted all three.
